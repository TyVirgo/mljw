# 输入模板（复制后填写）

> 复制为 `runs/<产品简称>/inputs.md` 再填。  
> **不必**写死「系统概要设计」或「第四/五章」——以本次实际模板与章节为准。  
> **缺下列必填项或结构未确认时，Agent 只提示并等待拍板，不生成 Word。**

## 1. 基本信息

| 项 | 填写 |
|----|------|
| 产品/项目名称 | |
| 文档类型（模板叫什么就填什么） | 如：系统概要设计说明书 / 详细设计说明书 / … |
| 对标视角（如权限平台、运维监控平台） | |
| 输出目录 | |
| 优先覆盖的文件名 | |
| 占用时备用文件名 | |

## 2. 模板与要写的章节（必填）

| 项 | 填写 |
|----|------|
| 文档模板路径 | |
| 要写的章节（章号+标题，可多章） | 例：第四章 模块功能分配；第五章 接口设计 |
| 其他章是否生成 | 否（默认） |
| 模板目标章目录是否已确认 | 是 / 否（否 → 先出目录树等拍板） |

## 3. 参考资料

| 项 | 路径 |
|----|------|
| 对标设计/需求/手册 | |
| 用途说明 | 仅作文风与表述参考，不扩展功能范围 |
| 用户是否点名必须对标 | 是 / 否 |
| 若点名却暂无材料 | 补路径后再写 / 确认放弃对标仅用参数 / （其它说明） |

> 点名为「是」且路径为空、又未选「放弃对标」时：**不得成稿**。

## 4. 本次写入的功能菜单

按写入顺序填写。子能力挂接方式取决于第 7 节结构约定（概要默认挂在「功能详细描述」下；详细设计常为 `3.5.x.y` 直接挂子功能）。

### 菜单 1

- 菜单名称：
- 对应参数编号（如有）：
- 子能力列表：
  1.
  2.

### 菜单 2

- 菜单名称：
- 对应参数编号（如有）：
- 子能力列表：
  1.

（按需追加…）

## 5. 功能参数原文（必填）

> 粘贴或附件。成稿须覆盖其中的操作与字段；正文不得整段照搬「需支持…」，且不得写成 AI 套话。  
> **无参数原文不得成稿。**

```
（粘贴参数需求）
```

## 6. 接口类章节约定（仅当目标章节含接口设计时）

| 项 | 填写 |
|----|------|
| 是否编写接口小节 | 是 / 否 |
| 体例 | 概要三列表（模块名/接口名称/path） / 详细设计接口明细（地址/方式/示例/参数表） / 以模板为准 |
| API 前缀（可选） | |
| path 是否仅占位 | 是 / 否 |

## 7. 结构与特殊约束

- 结构约定：  
  - [ ] 采用概要类默认（模块划分 / 功能列表 / 功能详细描述）  
  - [ ] 采用详细设计类骨架（见 design D2b / 对应 runs）  
  - [ ] 其它（请写出目录树或说明）：
- 必须写明的约束（删除限制、审核、失败重试等）：
- 明确不要写的内容：
- 图示：占位图题 / 不写图 / （其它）
