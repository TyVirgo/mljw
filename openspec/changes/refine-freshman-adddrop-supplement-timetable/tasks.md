# 任务：refine-freshman-adddrop-supplement-timetable

> 实现时按 P0 → P1 → P2。每项完成后勾选。不新增侧栏菜单。

## 1. 规格与演示数据基线

- [x] 1.1 确认本变更与 `align-ge-me-senior-freshman-model` 边界：本包不做双受众轮次/名额初分，只消费新生/老生判定与补注册名单
- [x] 1.2 为演示批次补充长学期/短学期标识或启发式，并准备对齐周五 17:00:00 的申请窗 demo 数据
- [x] 1.3 为补注册名单行预留 `inviteSentAt` / `deadlineAt` / `inviteAttempt` 等字段默认值

## 2. P0 — 新生闸门与截止对齐

- [x] 2.1 加退课页：新生且不在白名单 → 只读说明 + 引导在线选课，禁止提交
- [x] 2.2 新生在白名单且仅 `canAdd` → 仅开放加课申请入口
- [x] 2.3 选课监控：学分 &lt; 12 新生可「建议加入补注册」，确认后入名单；不自动开放加退
- [x] 2.4 批次表单/列表：长学期 `dropDeadlineWeek=2`、短学期 `=1`；申请窗结束对齐对应周周五 17:00 demo
- [x] 2.5 流程指引或批次 tip：区分「系统三轮」与「加退课申请窗」

## 3. P1 — 补注册 24h×2 与课程点码/一览

- [x] 3.1 补注册名单：发送邀请 → 写 `inviteSentAt`，`deadlineAt = +24h`，`inviteAttempt = 1`，展示模拟邮件 Callout
- [x] 3.2 学生端：常规窗外但名单有效且未逾期 → 可提交对应权限申请；展示倒计时/截止
- [x] 3.3 逾期锁定；「再次开放」→ attempt=2，新发送时刻再 +24h；第二次逾期提示人工处理
- [x] 3.4 可选演示：「设为已逾期」便于验收，不进入生产语义
- [x] 3.5 在线选课主表：课程代码可点击打开 `StudentCourseDetailDrawer`（或等价）
- [x] 3.6 课程一览：按 code 排序，仅当前学生可选范围；挂在线选课（抽屉或面板）

## 4. P2 — 多阶段课表与课程库入口

- [x] 4.1 抽取或复用课表预览组件，统一挂载数据契约（已确认 / 篮内预览 / 拟变更）
- [x] 4.2 在线选课：选课前与 R1 预选中可看课表预览
- [x] 4.3 结果公开后 / 我的选课结果：课表反映已确认课
- [x] 4.4 加退课页：拟加拟退后课表预览
- [x] 4.5 在线选课顶栏：课程库文档名列表 demo（ME/GE Library + 学年学期 + 版本日期）
- [x] 4.6 中英 i18n 补齐本变更全部用户可见文案

## 5. P2 修订 — 课表三色图层

- [x] 5.1 Demo 1～2 门必修（字段同选修确认课，`sourceType: required`），不接培养方案来源
- [x] 5.2 `buildPreviewSchedule` / 槽位：`layer` = required | confirmed | preview；透传 roundKey、组号、教室等
- [x] 5.3 `WeekScheduleGrid`：三色 + 轮次角标 + 悬停详情；预览虚线；抽屉图例
- [x] 5.4 R1：未公示志愿进预览层；公示后中签进已确认层，落选不占格
- [x] 5.5 在线选课 / 我的结果 / 加退课预览挂接；更新 proposal/design/spec

## 5b. 课表信息密度与入口卡间距

- [x] 5b.1 课表格：课号 + 中文名 + 教师 + 地点 + 起止周（rowspan 块）；demo 中文课名
- [x] 5b.2 入口卡纵向间距约收 ⅓（gap / padding / min-height）

## 5c. 课表全周晚课与按轮过滤

- [x] 5c.1 时段至 22:00；补齐周六日列（周末浅底）
- [x] 5c.2 按当前轮过滤：仅 ≤ 当前轮；虚线=本轮选择，实线=既往确认/必修
- [x] 5c.3 中文表头/格内中文；字号+1；格内居中；必修/选修底色区分

## 5d. 课表密度与排课文案格式（增量）

- [x] 5d.1 `WeekScheduleGrid`：行高收紧；左轴时间段；8–22；课块 5 行（周第 5 行）；地点 `#`
- [x] 5d.2 `formatVenueDisplay` + 在线选课等地列同步；`deriveClassTime` 去节次并中英分格式
- [x] 5d.3 勾选完成；冒烟课表/选课列表/上课时间列

## 6. P3 — GE/ME 学期进度与受众英文

- [x] 6.1 英文受众文案：Existing students / New students（用户可见处）
- [x] 6.2 Demo 学期学分帽抬高（默认 GE≤12、ME≤16）；导出已选/上限进度（含篮）
- [x] 6.3 在线选课顶栏右侧：GE/ME 进度条（已选/上限）
- [x] 6.4 超类型学期上限：选课按钮置灰 + 感叹号 tip（文案对齐 termCreditCapExceeded）
- [x] 6.5 监控详情：文科/商科进度改为 GE/ME 修读进度
