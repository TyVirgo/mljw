## 1. 数据与计费

- [x] 1.1 新增科类单价配置（文 500 / 理 550 / 商 600）与 `feeStream` 映射；实现应收学分与费用试算函数
- [x] 1.2 新增加课资格 demo 池（曾 Drop / 休学落下 / 排除后续学期）及往期成绩单 demo
- [x] 1.3 扩展申请 form/payload：sectionId、classTime、venue、lecturers 分字段、feeEstimate、重修成绩单引用；校验须已选分组

## 2. 选择器与表单

- [x] 2.1 改造加退选课器为「先课再组」两步；分组行展示时间/地点/教师/余量
- [x] 2.2 分组冲突检测：冲突组禁用并提示冲突课程与时段；退课后可再选
- [x] 2.3 表单分字段回填时间/地点/教师；加课走资格池；重修支持成绩单选择与类型默认
- [x] 2.4 声明前展示费用摘要；提交写入试算结果；Notes 补充资格与学期规则

## 3. 列表、详情与 i18n

- [x] 3.1 学生申请主表增加分组/时间/地点/教师/费用等列，冻列 + 横向滚动
- [x] 3.2 审批列表同步关键列与冻列横滑
- [x] 3.3 详情抽屉按分字段与费用/重修类型只读展示
- [x] 3.4 补充中英文 i18n；冒烟：选组、冲突禁用、加课资格、重修成绩单、费用试算、列表横滑

## 4. 学生端类型 Tab 与分列（增量）

- [x] 4.1 学生页增加 Add/Drop/Retake/AddDrop 四 Tab；列表按 Tab 过滤；搜索去掉类型筛选项
- [x] 4.2 按类型配置主表列并渲染；表头 `nowrap`；冻列改为序号+单号 / 状态+操作（去掉类型冻列）
- [x] 4.3 发起申请锁定当前 Tab 类型；表单申请类型只读；白名单不允许时禁用发起
- [x] 4.4 补充 Tab 相关 i18n（若需）；冒烟四 Tab 分列与锁定新建

## 5. 起止周与排课 demo（增量）

- [x] 5.1 各类型主表增加起止周列；列表/审批读取 weekRange；表头 nowrap
- [x] 5.2 种子与审批队列按图示样本补齐教师/周/时间/地点；缺字段可重种；列表不展示空「—」
- [x] 5.3 选组回填与详情同步 weekRange；展示时间走 deriveClassTime；勾选完成

## 6. Notes / 状态胶囊 / 审批同步（增量）

- [x] 6.1 表单 Notes 按 action 删减并重编号展示
- [x] 6.2 学生与审批列表状态颜色胶囊；补充 Pending/In Review 样式映射
- [x] 6.3 demo：五状态各 ≥2 条、四类型均有差异行；缺覆盖可重种
- [x] 6.4 审批列固定含状态+申请类型；类型筛驱动动态列与学生对齐；冒烟

## 7. 课程分组名称与选课弹窗收口（增量）

- [x] 7.1 申请/审批列表与表单：「教学分组/Group No.」改为「课程分组」，单元格展示分组名称（如分组名称01）
- [x] 7.2 选课弹窗仅选课程；底部「下一步」改为「确认」；分组改由表单下拉选择（冲突组禁用）
- [x] 7.3 弹窗搜索区左标签右输入；去掉资格来源列；加课场景增加资格 tip
- [x] 7.4 demo/enrich 补齐 sectionName；i18n；冒烟

## 8. 表单学分与说明收口（增量）

- [x] 8.1 加课区「加课类型」改为只读「学分」，由所选课程带出；去掉 addType 必填校验
- [x] 8.2 Notes 按类型仅保留实用规则并顺序编号；去掉分节填写指引与费用长文
- [x] 8.3 去掉零费用灰字；Section V 增加分类型其他申请说明；i18n

## 9. 操作列与表单字号（增量）

- [x] 9.1 学生主表操作冻列加宽；详情/取消横排 nowrap；状态冻列 right 偏移同步
- [x] 9.2 分节标题大于正文一档；标签/输入/Notes/提示字号收口；详情抽屉对齐；四类型共用

## 10. 课表入口与申请弹窗字号（增量）

- [x] 10.1 主页「发起申请」旁增加「我的课表」；抽屉复用课表预览；弹窗去掉内嵌课表
- [x] 10.2 申请弹窗内标题/分节/标签/输入/提示整体小一号

## 11. 上课时间地点多行 / 藏重修 / Notes 分类型 / 声明对齐（增量）

- [x] 11.1 主表列 classTime+venue → classTimeVenue；demo 多段 meetings；列表多行展示
- [x] 11.2 学生端 Tab 去掉 Retake；Section IV 不再露出；三 Tab 自适应
- [x] 11.3 Notes 标题按申请类型展示；声明勾选与文案同行对齐
- [x] 11.4 审批列表同步 classTimeVenue 列（若仍用同源列配置）
- [x] 11.5 恢复学生端「重修」Tab（Add/Drop/Retake/AddDrop）

## 12. 选课弹窗按分组展平 + SECTION 连续编号（增量）

- [x] 12.1 `AddDropCoursePickerModal`：按 section 展平行；列含分组/教师/起止周/上课时间地点；冲突行禁用
- [x] 12.2 确认回填 course+section；`handleCoursePicked` 调用 `applySectionToForm`
- [x] 12.3 表单/详情 SECTION 按可见节动态 I…N；i18n；勾选完成

## 13. 加课选课学分说明 / 筛选 / 学期只读（增量）

- [x] 13.1 弹窗搜索同行：课程代码 + 学分 + 课程类型
- [x] 13.2 搜索下展示本学期学分胶囊；超额确认提示（可继续）
- [x] 13.3 表单学年学期只读默认当前学期；i18n；勾选完成

## 14. 选课弹窗排版与超额双提示（增量）

- [x] 14.1 搜索区标签+控件同一行；去掉学分条下静态费用句
- [x] 14.2 页脚确认左侧条件超额提示 + 确认二次弹窗；i18n
- [x] 14.3 demo：仅 1 组冲突、1 门可选可触发超额；勾选完成

## 15. 校选类别列/筛与间距（增量）

- [x] 15.1 主表「课程类型」前增加校选类别列；搜索区同位置增加筛选
- [x] 15.2 压缩学分胶囊灰底上下内边距、上课时间地点多行行距；勾选完成

## 16. 超额确认层级与表格紧凑（增量）

- [x] 16.1 ConfirmDialog 支持 zIndex；超额确认高于选课弹窗（≥1300）
- [x] 16.2 主表除上课时间地点外 nowrap + 横滑；单元格间距收紧；勾选完成

## 17. 加课节预估费用字段（增量）

- [x] 17.1 加课/重修分节增加只读预估费用 + 1学分单价 tip；保留底部费用汇总；i18n；勾选完成

## 18. 超出学分与加课费用块上移（增量）

- [x] 18.1 加课节：教师|学分、超出学分|预估费用；费用块移到补充说明上（仅 Add/AddDrop）
- [x] 18.2 列表/详情/payload 同步超出学分；重修费用块位置不变；i18n；勾选完成

## 19. 文商理超额同源 + 审批不拦（增量）

- [x] 19.1 `estimateCourseFee` / `planRemaining`：GE/ME 按文商理类别剩余计 `billableCredits`；弹窗页脚、表单超出学分与费用同源
- [x] 19.2 审批去掉超分硬拦（单条/批量）；详情展示超分与费用即可通过
- [x] 19.3 审批提交 ConfirmDialog z-index 高于审批弹窗；冒烟：COMP201 文 4 分 → 超出 1、费用 500、可批过

## 20. 审批失败文案 + 生成账单说明（增量）

- [x] 20.1 修正 `batchPartialFail` 等文案，去掉「学分超限」暗示；侧写失败不影响已通过状态落库
- [x] 20.2 「生成账单」常驻可见说明（关联缴费名单）；费用识别含 billAmount/excessCredits；冒烟

## 21. 退课校选类别 / 分组只读 / 成功提示位（增量）

- [x] 21.1 我的选课历史增加校选类别列；已选课映射带出 type/schoolElectiveCategory
- [x] 21.2 退课选课弹窗校选类别与历史同源；退课表单课程分组改只读带出
- [x] 21.3 提交成功提示与发起申请同行最右；冒烟
```
