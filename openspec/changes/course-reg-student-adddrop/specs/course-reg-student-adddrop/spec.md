# 选课管理-学生加退课 规格（合并）

## 说明

以下为各历史 delta spec 原文按来源拼接并做术语中文化处理。整理正式需求文档时，应按场景重写为「用户故事 / 验收标准」，消除重复与过时条目。

## 来源 `add-adddrop-course-picker` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 加退课申请以表格单选选择课程
学生加退课/重修申请弹窗的课程字段 必须NOT 使用下拉框列举全部课程。系统 必须提供类似课程库导入的搜索表格选择器，且 必须为单选（一门申请对应一门课）。加课/重修候选 必须来自可选课；退课候选 必须来自已选课。

#### Scenario: 打开课程选择器
- **WHEN** 用户在申请弹窗点击课程「选择」
- **THEN** 打开课程选择器，可按代码/名称搜索并分页浏览候选课

#### Scenario: 单选确认回填
- **WHEN** 用户选中一行并确认
- **THEN** 选择器关闭，申请表单课程字段显示该课代码与名称
- **AND** 提交时使用该课作为申请目标

#### Scenario: 退课候选为已选课
- **WHEN** 申请类型为退课
- **THEN** 选择器列表仅为该生已选课程（可含教学分组与时间）

## 来源 `enrich-student-adddrop-form` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 学生加退课申请表单字段
学生发起加退课/重修申请时 必须选择学年学期、申请类型与课程，并填写申请说明；申请类型文案 必须使用「申请类型」而非「操作类型」；Drop 申请 必须选择退费抵免（是/否）；超期退课 必须额外上传附件。

#### Scenario: 表单步骤与 Drop 字段
- **WHEN** 学生打开发起申请弹窗
- **THEN** 可见学年学期、申请类型、课程、申请说明
- **AND** 选择退课时可见退费抵免是/否
- **AND** 处于超期退课通道时附件为必填

#### Scenario: 提交落库字段
- **WHEN** 学生成功提交申请
- **THEN** 申请单含 academicSession、reason，Drop 含 feeWaiver

## 来源 `polish-adddrop-form-layout` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 加退课申请弹窗布局与 tip 样式
学生加退课申请弹窗 必须采用两列表单布局；退费抵免 必须以单选项展示；必填星号 必须为红色；说明 tip（原灰色 rule）必须使用浅蓝色底与感叹号图标，与其它提示条视觉一致。

#### Scenario: 弹窗字段布局
- **WHEN** 学生打开发起申请弹窗
- **THEN** 学年学期与申请类型等同排两列展示
- **AND** 退费抵免为是/否单选而非下拉
- **AND** 必填标签星号为红色

#### Scenario: tip 样式
- **WHEN** 页面展示 rule 类说明条或弹窗 Drop 通道说明
- **THEN** 底色为浅蓝且图标为感叹号

## 来源 `polish-adddrop-label-intake-desc` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 加退课文案与菜单一致
批次表单加退课时间区块与批次列表对应列标题 必须使用「加退课/重修申请」，与学生菜单文案一致。

### Requirement: 批次类选项倒序
学年学期与学生范围「批次」下拉中的日期型选项 必须按新到旧倒序排列；「全部」或「请选择」必须仍位于列表最前（由 UI 固定）。

## 来源 `polish-demo-teaching-week-callout` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 演示教学周与提示条一体展示
学生加退课页原型期的「演示教学周」控件 必须与通道说明位于同一 tip 条带内：共用底色与边框，标签字体与说明协调；必须NOT 在条带外单独成白底区块。

#### Scenario: 整行一体
- **WHEN** 学生打开加退课页
- **THEN** 通道说明与演示教学周处于同一连续底色行内
- **AND** 演示周标签字色/字号与说明文案协调

## 来源 `polish-student-adddrop-list` / 能力 `course-registration`

## ADDED Requirements

### Requirement: 学生加退课列表布局与 demo
系统 必须在学生加退课页提供标准搜索区与 toolbar 发起申请；通道提示与演示教学周 必须同一行且演示选项靠右；申请列表 必须分页展示，demo 覆盖各类型/状态/退课通道；非退课类申请退课通道列 必须显示「非退课申请」而非占位符。

#### Scenario: 搜索与发起申请位置
- **WHEN** 学生打开加退课/重修申请页
- **THEN** 可见类型、状态、关键词搜索与查询/重置
- **AND** 「发起申请」位于搜索区下方 toolbar

#### Scenario: 列表数据与退课通道列
- **WHEN** 列表渲染 demo 申请
- **THEN** 可见分页且涵盖 Add/Drop/Retake/Replace/AddDrop 与多种审批状态
- **AND** 退课类显示自助或特殊审批；加课/重修等显示「非退课申请」

## 增量 2026-08 / 能力 `course-registration`

### Requirement: 加课选课弹窗展示 GE 与 ME 文商理细化

系统 SHALL 在加课（及加退关联选课）课程选择器中展示本学期学分方案 B：公共选修总分与文/商/理，以及专业选修总分与文/商/理。GE 文商理要求之和对齐 geMax；ME 文商理要求之和对齐 meMax。

#### Scenario: 加课弹窗可见双组细化
- **WHEN** 学生打开加课申请的选择课程弹窗
- **THEN** 可见 GE 已选/要求与文/商/理胶囊，以及 ME 已选/要求与文/商/理胶囊

### Requirement: 加退课申请不进入在线选课排队并锁定名额

系统 SHALL NOT 因学生提交加课、退课、重修或加退关联申请而进入在线选课排队。仅正常在线选课提交 SHALL 进入排队队列。学生提交含加课或重修且所选教学组有余量时，系统 SHALL 立即锁定 1 个名额并将申请送入管理端审批队列。学生在审批开始前取消或管理端拒绝时，系统 SHALL 释放该名额。审批通过且需缴费时，系统 SHALL 以选课规则延迟缴费天数（CR107，默认 2）计算缴费截止日。

#### Scenario: 加课提交不排队
- **WHEN** 学生提交加课申请且所选分组有余量
- **THEN** 不展示选课排队蒙层
- **AND** 该分组已占用名额增加 1
- **AND** 申请出现在管理端加退课审批列表

#### Scenario: 重修与加课同口径
- **WHEN** 学生提交重修申请且所选分组有余量
- **THEN** 行为与加课相同：不排队、锁名额、进入审批

#### Scenario: 审批通过缴费宽限
- **WHEN** 管理端批准需缴费的加课或重修申请
- **THEN** 申请记录缴费截止日为通过时刻起 CR107 天数之内
- **AND** 不进入在线选课排队

## 增量 2026-08 加课文商理与列表展示 / 能力 `course-registration`

### Requirement: 文科学生仅可见文科专业选修且公共选修含文商理

系统 SHALL 将未标注校选类别的专业选修视为文科。当前演示生默认为文科时，加课选择器与在线选课列表 SHALL 只展示文科专业选修。公共选修 SHALL NOT 按学生科类过滤，加课候选 SHALL 同时包含文科、商科、理科公共选修。时间冲突的教学组状态文案 SHALL 为「时间冲突」。

#### Scenario: 文科生加课候选
- **WHEN** 默认文科学生打开加课课程选择器
- **THEN** 可见的专业选修均为文科
- **AND** 可见的公共选修覆盖文、商、理
- **AND** 除 COMP201 分组 03 外，演示候选分组不因课表冲突而禁用

#### Scenario: 冲突文案
- **WHEN** 教学组与已选课表时间重叠
- **THEN** 名额状态显示「时间冲突」（英：Time conflict）

### Requirement: 审批已提交与历史无勾选时冻结列不错位

管理端加退课审批待审批 Tab SHALL 显示行勾选；已提交与历史 Tab SHALL NOT 显示勾选。无勾选时冻结列偏移 SHALL 按无勾选列宽计算，申请单号 MUST NOT 覆盖状态列。审批三 Tab SHALL NOT 展示「当前学分/上限」学分列。

#### Scenario: 已提交冻结列
- **WHEN** 用户切换到已提交或历史
- **THEN** 表头无勾选列
- **AND** 序号贴左、申请单号紧随其后，状态列完整可见

### Requirement: 上课时间地点按时段整行展示

加退课审批列表、学生加退课列表与加课选择器的「上课时间地点」SHALL 按时段分行；同一时段文本 SHALL 不在单元格内折行。

### Requirement: 超出学分与费用账单一致

列表中的超出学分、预估费用与账单状态 SHALL 同源：超出学分大于 0 时才展示非零费用与缴费状态；超出学分为 0 时费用为 0、账单为「—」。

#### Scenario: 未超出学分
- **WHEN** 申请净加课后仍不超过学分上限（或类型为纯退课）
- **THEN** 超出学分为 0
- **AND** 预估费用为 0
- **AND** 账单列为「—」

## 增量 2026-08 选择器冻结与详情 / 能力 `course-registration`

### Requirement: 加课选择器冻结状态列且默认每页 20 行

加课/退课/重修课程选择器 SHALL 将「状态」列右侧冻结；默认分页 SHALL 为每页 20 行。

### Requirement: 审批详情不展示学分摘要与校验结果

管理端加退课审批详情 SHALL NOT 展示「专业/入学批次 · 学分 x/y」条，SHALL NOT 展示「校验结果」区块。

### Requirement: 详情学年学期与联系电话必有值

加退课详情的学年学期 SHALL 为该申请提交时的学年学期；联系电话 SHALL 为申请人联系电话。两者 MUST NOT 显示为「—」。

### Requirement: 详情课程名称与编码分列

加课、退课、重修详情的课程字段 SHALL 分两列：左为课程名称、右为课程编码。列表列格式保持「课程编码 + 课程名称」。

### Requirement: 费用不重复且金额带 RMB

详情课程分区 SHALL NOT 再展示「预估费用」字段；费用以「费用预估」合计为准。加退课列表、表单与详情中的金额 SHALL 带单位 RMB。

### Requirement: 详情学生声明与申请时一致

详情学生声明 SHALL 展示申请时的说明文案、浅黄底声明框、勾选框与同意语句；勾选框只读。

#### Scenario: 打开审批详情
- **WHEN** 管理员打开加课申请详情
- **THEN** 不见学分摘要条与校验结果
- **AND** 学年学期与联系电话有值
- **AND** 加课区左名称右编码
- **AND** 声明区含底色、说明与勾选框

### Requirement: 重修节先选本学期课程并带回曾修信息

重修申请分节 必须 先选择本学期重修课程，必须NOT 展示「从往期成绩单选择」或「曾修课程（课号与课名）」。选课后 必须 带回曾修成绩。曾修学年学期 必须 默认为申请学年学期的上一学期（学期月为 02/04/09）。第三行 必须 左为重修类型、右为课程分组。选择分组后 必须 带回起止周、上课时间地点、教师、学分与费用。

#### Scenario: 打开重修表单不见成绩单入口
- **WHEN** 学生打开重修申请
- **THEN** 不见从往期成绩单选择与曾修课程字段
- **AND** 首个业务字段为本学期重修课程
- **AND** 曾修学年学期已填为申请学年学期的上一学期

#### Scenario: 选课带回成绩与课信息
- **WHEN** 学生选定本学期重修课程及分组
- **THEN** 曾修成绩来自该课对应往期记录
- **AND** 曾修学年学期仍为上一学期
- **AND** 重修类型与课程分组同一行
- **AND** 起止周、上课时间地点、教师、学分与费用已带回

### Requirement: 加退关联类型先屏蔽

学生加退课页与管理端审批 必须NOT 展示加退关联类型。可见申请类型 必须 仅为加课、退课、重修。已有加退关联 demo 记录 必须NOT 出现在列表、计数或导出中；学生 必须NOT 能新发起加退关联。

#### Scenario: 学生页不见加退关联 Tab
- **WHEN** 学生打开加退课/重修申请
- **THEN** 类型 Tab 仅为加课、退课、重修

#### Scenario: 审批队列不含关联单
- **WHEN** 管理员打开加退课/重修审批
- **THEN** 待审批计数与列表不含加退关联
- **AND** 类型筛选无加退关联选项

### Requirement: 重修节对齐问卷 SECTION IV 字段

重修申请分节 必须 覆盖问卷 SECTION IV 所需信息：本学期重修课程（含课号课名，代替曾修课名）、曾修成绩、曾修学年学期、重修类型、课程分组、上课时间地点、任课教师。必须NOT 再单独要求填写曾修课程名称。曾修成绩与曾修学年学期 必须 为必填可选列表。上课时间地点与任课教师在分组无数据时 必须 允许手填，并 必须 提供问卷式示例提示。

#### Scenario: 成绩学期可改
- **WHEN** 学生已选本学期重修课
- **THEN** 曾修成绩与曾修学年学期为下拉且可改
- **AND** 学年学期默认值为申请学期的上一学期

#### Scenario: 无课表数据时可手填
- **WHEN** 所选分组没有上课时间地点或教师
- **THEN** 对应字段为空输入框并显示示例提示
- **AND** 未填写不得提交

