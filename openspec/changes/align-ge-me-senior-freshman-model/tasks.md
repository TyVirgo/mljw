# 任务：align-ge-me-senior-freshman-model

> 全部任务均须完成方可验收；Phase 仅表示推荐实施顺序。每 Phase 交付时做一次 UI 并排自检。

## Phase 1 — 底座：身份、双时间、名额

- [x] 1.1 学生上下文增加注册学期序号；新生/老生判定；i18n 与流程指引写明 GE/ME 与身份定义
- [x] 1.2 批次数据模型增加 `roundsByAudience`（senior/freshman）；兼容迁移现有 `rounds`
- [x] 1.3 管理轮次抽屉：老生 | 新生 Tab；各 R1–R3 起止；老生 R1 结果发布时间；放宽跨受众重叠校验
- [x] 1.4 批次列表轮次列展示两套摘要；demo 按邮件表灌至少一套学年学期
- [x] 1.5 管理课程：新老名额可编辑保存；「按比例初分」实现图示1；year2/sem2 课新生名额强制 0 + tip
- [x] 1.6 实时已选/名额列保持正确；课详情增加往期占比 demo 表；可选扩「名额分配」弹窗（样式对齐容量设置）
- [x] 1.7 Phase 1 UI 自检：与现有抽屉/表格/按钮一致；中英齐全；无新侧栏菜单

## Phase 2 — 学生窗口、学分帽、R3 互释

- [x] 2.1 在线选课按受众读取对应轮次时间窗；轮次条/提示区分新老
- [x] 2.2 学生 GE/ME 学期学分上限字段 + 加篮/提交校验与提示
- [x] 2.3 实现 R3 双向释放对方预留剩余（开关 + mock 容量计算）；手调名额仍可用
- [x] 2.4 Phase 2 UI 自检

## Phase 3 — 第一轮加权自动发布

- [x] 3.1 规则设置：默认 r（GE 默认 3）、可编辑；ME 按专业 r 覆盖简易表
- [x] 3.2 抽签引擎 mock：老生名额池、毕业生优先、按日权重加权随机、同课多组互斥
- [x] 3.3 选课结果第一轮：自动发布主路径（到点或一键）；最终确认降级为可选；与 R2 闸门衔接
- [x] 3.4 志愿抽屉/结果文案对齐「无人干预主路径」；保留必要运维提示
- [x] 3.5 Phase 3 UI 自检 + 全量对照老师清单回归（时间、名额、抽签、学分、互释、无新菜单）

## 收尾

- [x] 4.1 流程指引补齐双时间线、名额初分、自动发布、R3 互释说明
- [x] 4.2 tasks 全部勾选；可归档或 sync 规格

## Phase 4 — 时分秒精度与列表标签

- [x] 4.3 `DatePickerEn` 支持 datetime（`DD/MM/YYYY HH:mm:ss`，秒默认 00）；轮次/结果发布/加退课申请窗使用
- [x] 4.4 批次存储/互转/formatRoundRange 兼容旧纯日期（展示补 00:00:00）；申请窗比较精确到秒
- [x] 4.5 批次列表：新生：/老生：换行完整标签 + 时分秒；结果发布与轮次格式一致
- [x] 4.6 加退课/志愿等 submittedAt 展示与新写入补秒；中英 i18n placeholder 同步
- [x] 4.7 Phase 4 UI 自检

## Phase 5 — 列表密度与提示精简

- [x] 5.1 在线选课去掉身份条、学分规则 Callout；主表行距收紧以多展示数据行
- [x] 5.2 批次列表：名称等列不换行 + 横向滚动；轮次列保留新生/老生两行；整体行距压到不遮挡内容
- [x] 5.3 Phase 5 UI 自检
