## 1. 数据层

- [x] 1.1 将 `registrationRuleSettings.js` 默认目录改为 CR201–CR204 + CR107；STORAGE_KEY 升为 v3
- [x] 1.2 保留并确认 `getPaymentGraceDays` 仍读 CR107；旧 CR101–CR106 不再出现在目录中

## 2. i18n 与文档

- [x] 2.1 更新 zh/en `registrationRules.items` 文案（新四条 + 保留延迟缴费；旧键可注释弃用说明）
- [x] 2.2 在 `course-reg-rules` 合并包 proposal 补本增量说明

## 3. 验收

- [x] 3.1 确认设置页仍三列、恰五条；批次表单本轮不改
