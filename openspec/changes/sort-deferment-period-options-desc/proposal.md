## 背景与动机

休学申请「休学期间（学年学期）」下拉当前按学年学期升序（旧→新），选近期学期需往下翻。产品要求改为倒序：新的学年学期在前，旧的在后。

## 变更内容

- `getDefermentPeriodOptions` 按学年学期倒序生成选项（占位「请选择」仍置顶，由表单 option 单独渲染）

## 能力范围

### 修改的能力

- `deferment-app`：休学期间下拉选项排序

## 影响范围

- `src/data/deferments.js`

## 非目标

- 不改其它模块学年学期下拉排序（除非共用同一函数）
- 不改已选值语义 / 起止日期联动
