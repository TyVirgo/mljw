# 任务：unify-whitelist-and-me-batch-programme

> 实现时按序勾选。不新增并列「补注册」菜单。加课申请不得破容量。

## 1. 规格与数据模型

- [x] 1.1 定义白名单权限枚举与分权限时效字段（默认 validHours=24，支持 validUntil）
- [x] 1.2 迁移 `supplementListQueue` demo 行：旧 canAdd/Drop/Retake + 整行邀请 → `permissions[]`
- [x] 1.3 合并/对齐旧 `whitelistQueue` 类型语义到权限（先修、学分等），避免双列表真相源
- [x] 1.4 ME demo 批次补充 `programme`；GE 批次不加该必填

## 2. 菜单与白名单管理页

- [x] 2.1 侧栏「补注册」改为「白名单」（i18n 中英）；去掉并列补注册入口
- [x] 2.2 白名单列表：权限摘要、各权限到期、状态；筛选/搜索保持可用
- [x] 2.3 新增/编辑：权限多选 + 每权限时长/截止配置 + 发送邀请/再次开放
- [x] 2.4 监控「建议加入补注册」文案改为「建议加入白名单」并写入对应权限默认（如补注册+加课）

## 3. 学生加退闸门与申请校验

- [x] 3.1 `getAddDropAccess`（或等价）：窗外依白名单分权限有效性放行对应申请类型
- [x] 3.2 加课申请选组：满容不可选/不可提交（白名单不破容量）
- [x] 3.3 先修豁免：仅加课申请路径读取；在线选课资格评估保持先修校验
- [x] 3.4 学生端提示：白名单仅开门交表；审批/缴费另计（短说明即可）

## 4. ME 批次专业字段

- [x] 4.1 批次新建/编辑：类型 ME 时展示并校验必选专业；GE 隐藏/不校验
- [x] 4.2 批次列表/详情展示 ME 所属专业
- [x] 4.3 保存与 mock 数据读写 `programme`

## 5. 收尾

- [x] 5.1 流程指引或 Callout：白名单权限、分时效、先修破例路径、容量不破
- [x] 5.2 中英 i18n 补齐本包用户可见文案
- [x] 5.3 勾选本 tasks；与 `refine-freshman-adddrop-supplement-timetable` 文案冲突处统一为白名单
