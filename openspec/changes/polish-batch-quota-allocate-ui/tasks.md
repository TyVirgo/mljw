## 1. 名额分配弹窗

- [x] 1.1 在册新老只读一行两列；去掉按比例初分按钮；打开自动初分
- [x] 1.2 新老配额一行两列；合计不超过有效容量；多组按比例写回
- [x] 1.3 更新 hint / 校验提示 i18n

## 2. 管理课程表

- [x] 2.1 有效最大容量 / 新生容量 / 老生容量仅显示容量；表头与 tip 同步

## 3. 初分同步与弹窗收口

- [x] 3.1 默认/容量设置变更后按在册初分公式重算新老容量（与表同步）
- [x] 3.2 弹窗：公式底色 callout、去掉多余长说明；手改配额确定前二次确认
- [x] 3.3 页脚左侧增加「配额重置」，按初分公式回填新老配额

## 4. 手改配额持久化与重置

- [x] 4.1 `enrichCourse` / `distributeSectionAudience`：仅超额压回，允许合计 < 有效容量，不因不等而强制初分
- [x] 4.2 `update*AudienceQuota` 只写 `quota`，不改分组/课程 `capacity`
- [x] 4.3 弹窗打开优先已存配额；重置才强制初分；watch 仅跟 `visible`
